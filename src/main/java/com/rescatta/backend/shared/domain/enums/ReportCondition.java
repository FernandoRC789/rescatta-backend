package com.rescatta.backend.shared.domain.enums;

/** Estado del animal en el momento del reporte — corresponde a los chips "Estado del animal" del formulario. */
public enum ReportCondition {
    HERIDO,
    SANO,
    CACHORRO,
    ATROPELLADO,
    OTROS
}
